server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mainline.proxy.rlwy.net:24353/tienda_saas?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ArpCGngyCGwEcLItfWCoHpcjsIxssefI
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate    # Solo valida las tablas
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  # === CONFIGURACIÓN PARA SUBIDA DE ARCHIVOS (IMÁGENES) ===
  servlet:
    multipart:
      enabled: true                 # Asegura que multipart esté activado
      max-file-size: 15MB           # Máximo por imagen (ajusta si quieres más/menos)
      max-request-size: 50MB        # Máximo total del request (para varias imágenes)
      file-size-threshold: 2MB      # Opcional: escribe en disco temporal si >2MB

jwt:
  secret: 8f9e2a7c1d5b3e6f4a9d8c7b6e5f4d3c2b1a0f9e8d7c6b5a4f3e2d1c0b9a8f7e6d5c4b3a2910
  expiration: 86400000 # 24 horas

app:
  frontend-url: http://localhost:4200